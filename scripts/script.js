jQuery(document).ready(function($) {
  $('.fadeOut').owlCarousel({
    items: 1,
    center: true,
    animateOut: 'fadeOutLeft',
    // nav: true,
    dots: false,
    loop: true,
    margin: 10,
  });
});
